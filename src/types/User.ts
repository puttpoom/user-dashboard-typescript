export type UserCredential = {
  email: string;
  password: string;
};

export type UserData = {
  password: string;
  createdAt: string;
  name: string;
  avatar: string;
  aboutme: string;
  address: string;
  gender: string;
  email: string;
  id: string;
};

export type TFormInput = {
  email: string;
  password: string;
};
